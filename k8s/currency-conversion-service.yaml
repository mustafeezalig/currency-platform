apiVersion: v1
kind: Service

metadata:
  name: currency-conversion
  namespace: currency-prod

spec:
  selector:
    app: currency-conversion

  ports:
    - protocol: TCP
      port: 8100
      targetPort: 8100

  type: LoadBalancer